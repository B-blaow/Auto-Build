name: Build ImmortalWrt

on:
  schedule:
    - cron: "0 0 2 * *"
  workflow_dispatch:
    inputs:
      ssh:
        description: "Enable SSH debug (tmate)"
        required: false
        default: "false"

env:
  WORKDIR: /mnt/immortalwrt
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_NAME: immortalwrt
  SSH_ENABLE: ${{ github.event.inputs.ssh || 'false' }}
  CCACHE_DIR: /mnt/ccache_immortal
  CCACHE_MAXSIZE: 10G

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare Disk
        run: |
          sudo swapoff -a || true
          sudo rm -f /swapfile || true
          sudo mkdir -p /mnt
          sudo chown runner:runner /mnt

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk gettext gcc-multilib \
          g++-multilib git gnutls-dev gperf haveged help2man intltool lib32gcc-s1 libc6-dev-i386 libelf-dev \
          libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses-dev libpython3-dev \
          libreadline-dev libssl-dev libtool libyaml-dev libz-dev lld llvm lrzsz mkisofs msmtp nano \
          ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-ply python3-docutils \
          python3-pyelftools qemu-utils re2c rsync scons squashfs-tools subversion swig texinfo uglifyjs \
          upx-ucl unzip vim wget xmlto xxd zlib1g-dev zstd
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib \
          gettext git libssl-dev python3 python3-pip python3-venv rsync unzip zlib1g-dev \
          file wget ccache

      - name: Enable ccache
        run: |
          mkdir -p $CCACHE_DIR ~/.ccache
          echo "max_size = $CCACHE_MAXSIZE" > ~/.ccache/ccache.conf
          echo "export CCACHE_DIR=$CCACHE_DIR" >> $GITHUB_ENV
          ccache -z

      - name: Clone Source
        run: |
          mkdir -p $WORKDIR
          cd $WORKDIR
          git clone --depth=1 $REPO_URL $REPO_NAME

      - name: Create compatibility path (openwrt)
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          [ ! -d openwrt ] && ln -s $(pwd) openwrt || true

      - name: Run first custom script
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          set +e
          FILE=$(ls -1 $GITHUB_WORKSPACE/immortalwrt*.sh | sed -n '1p' || true)
          [ -f "$FILE" ] && chmod +x "$FILE" && "$FILE" || true

      - name: Copy config
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          cp $GITHUB_WORKSPACE/immortalwrt.config .config

      - name: Update & Install Feeds
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Run second custom script
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          set +e
          FILE=$(ls -1 $GITHUB_WORKSPACE/immortalwrt*.sh | sed -n '2p' || true)
          [ -f "$FILE" ] && chmod +x "$FILE" && "$FILE" || true

      - name: SSH Debug (Optional)
        if: env.SSH_ENABLE == 'true'
        uses: mxschmitt/action-tmate@v3

      - name: Build
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          make defconfig
          make -j$(nproc) || make -j1 V=s

      - name: Zip BIN
        working-directory: /mnt/immortalwrt/immortalwrt
        run: |
          zip -r /mnt/immortalwrt/immortalwrt_bin.zip bin

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt_bin
          path: /mnt/immortalwrt/immortalwrt_bin.zip

      - name: Create Tag
        run: |
          TAG=immortalwrt-$(date +"%Y%m%d-%H%M")
          echo "TAG_NAME=$TAG" >> $GITHUB_ENV
          git tag -f "$TAG"
          git push -f origin "$TAG"

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          files: /mnt/immortalwrt/immortalwrt_bin.zip
          tag_name: ${{ env.TAG_NAME }}
          name: "ImmortalWrt Auto Build ${{ env.TAG_NAME }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
